import { BulkEmailWithTemplate } from './bulk-email-with-template';

describe('BulkEmailWithTemplate', () => {
  it('should create an instance', () => {
    expect(new BulkEmailWithTemplate()).toBeTruthy();
  });
});
